{
    "configurations": [
      {
        "name": "Docker: Python - Flask",
        "type": "docker",
        "request": "launch",
        "preLaunchTask": "docker-run: debug",
        "python": {
          "pathMappings": [
            {
              "localRoot": "${workspaceFolder}/rna_dashboard/containers/www",
              "remoteRoot": "/app"
            }
          ],
          "projectType": "django"
        }
      }
    ]
  }
  
  .NE